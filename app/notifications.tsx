import { useEffect, useState } from 'react';
import { SafeAreaView, ScrollView, StyleSheet, Text, TouchableOpacity, View } from 'react-native';
import { useRouter } from 'expo-router';
import { supabase } from '@/lib/supabase';
import { currentProfile } from '@/lib/api';
export default function Notifications(){const router=useRouter();const [rows,setRows]=useState<any[]>([]);useEffect(()=>{(async()=>{const p=await currentProfile();const {data}=await supabase.from('notifications').select('*').eq('user_id',p.id).order('created_at',{ascending:false}).limit(50);setRows(data||[]);const unread=(data||[]).filter(x=>!x.read_at).map(x=>x.id);if(unread.length)await supabase.from('notifications').update({read_at:new Date().toISOString()}).in('id',unread)})()},[]);return <SafeAreaView style={s.page}><ScrollView contentContainerStyle={s.wrap}><View style={s.head}><TouchableOpacity onPress={()=>router.back()}><Text style={s.back}>رجوع</Text></TouchableOpacity><Text style={s.title}>الإشعارات</Text></View>{rows.length?rows.map(x=><View style={s.card} key={x.id}><Text style={s.ct}>{x.title}</Text><Text style={s.body}>{x.body}</Text><Text style={s.date}>{new Date(x.created_at).toLocaleString('ar-SA')}</Text></View>):<Text style={s.empty}>لا توجد إشعارات</Text>}</ScrollView></SafeAreaView>}
const s=StyleSheet.create({page:{flex:1,backgroundColor:'#F4F7FB'},wrap:{padding:20},head:{flexDirection:'row',justifyContent:'space-between',marginBottom:18},back:{color:'#159A74',fontWeight:'800'},title:{fontSize:24,fontWeight:'900',color:'#17233C'},card:{backgroundColor:'#fff',borderRadius:18,padding:16,marginBottom:10},ct:{textAlign:'right',fontWeight:'900',fontSize:16},body:{textAlign:'right',marginTop:6,color:'#596575'},date:{textAlign:'right',marginTop:9,color:'#9AA4B1',fontSize:11},empty:{textAlign:'center',color:'#8994A2',marginTop:50}});
